{
  "name": "criss-wang-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "refresh:stats": "node scripts/refresh-live-stats.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "clean": "rm -rf dist .astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.2"
  }
}
